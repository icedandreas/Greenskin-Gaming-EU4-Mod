namespace = greenskin_event

country_event = {
	id = greenskin_event.1
	
	trigger = {
		NOT = { has_opinion = {who = ROOT value = -35}}
	}
	
	mean_time_to_happen = {
		days = 1
	}
	
	immediate = {
		hidden_effect = {
			add_opinion = {
				who = ROOT
				modifier = opinion_improved_relations
			}
		}